/*
    Restaurant Menu Card e khabar er name+details deya thake.
    kon khabar pawa jabe Menu Card dekhe jana jay,
    but kivabe khabar ranna hocche seta Menu Card theke jana jay na.

    Similarly, 
    Header File is like Menu Card.
    Header File e function name + info thake.
    But function er real code(Implementation) onno kothao thake(Library).

    ~ .h extension mane holo header file

    
    ~Example: 
    #include<stdio.h>
    Ei Header File amader bole je Printf() & scanf() function use kora jabe.
    But how printf() works that code is not in stdio.h

    ~Why use header file:
    // header file e age theke banano function use kora jay
    // bar bar code likhte hoy na

*/

//===================================

// Important header files for C program
#include<stdio.h>       // printf(), scanf()
#include<stdlib.h>     // Dynamic Memory Allocation,Conversion,Program Termination
                        // malloc(), free(), exit()
#include<string.h>      // String Manipulation:- strlen(),strcpy(),strcmp()
#include<math.h>        // Mathematical Function:- sqrt(), pow(), sin()